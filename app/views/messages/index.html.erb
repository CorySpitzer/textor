<h2>Messages</h2>

<%= flash[:notice] %>

<%= link_to "New Message", new_message_path %>

<ul>
  <% @messages.each do |message| %>
    <li> <%= link_to message.body, message_path(message) %> </li>
  <% end %>
</ul>
